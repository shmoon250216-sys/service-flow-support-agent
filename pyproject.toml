[project]
name = "service-flow-support-agent"
version = "0.1.0"
description = "面向消费电子售后的可恢复 RAG 工单 Agent"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110", "pydantic>=2.6", "uvicorn>=0.27"]

[tool.setuptools]
package-dir = {"" = "src"}

