# 导学：售后工单

## 1. 前置知识

| 知识点 | 为何需要 | 在本项目中的位置 | 高频度 |
|---|---|---|---|
| HTTP、身份与权限 | 区分谁登录、谁能改工单 | src/service_flow/api.py | 高 |
| SQL 事务、唯一约束 | 状态、账本、事件共同提交 | src/service_flow/store.py | 高 |
| 状态机与乐观锁 | 合法流转、拒绝旧页面覆盖 | src/service_flow/cases.py | 高 |
| 幂等、重试、租约 | 崩溃恢复和避免重复消费 | src/service_flow/outbox.py | 高 |
| asyncio 与线程 | 理解后台扫描不阻塞事件循环 | src/service_flow/api.py | 高 |

## 2. 重点亮点与顺序

| 亮点标题 | 为什么重要 | 通用技术关键词 | 先看哪些文件 | 建议学习顺序 |
|---|---|---|---|---|
| 业务闭环 | 先知道用户怎么走完流程 | HITL、FSM | src/service_flow/conversations.py | 1 |
| 状态一致性 | 防止旧客服覆盖新处理 | CAS、RBAC、事务 | src/service_flow/cases.py | 2 |
| 动作安全 | 退款不能靠一句提示词授权 | 确认门、唯一约束 | src/service_flow/store.py | 3 |
| 通知可靠性 | 写成功后崩溃仍能通知 | Outbox、租约、幂等 | src/service_flow/outbox.py | 4 |
| 业务验证 | 证明机制而不是背名词 | Golden Dataset、故障注入 | scripts/evaluate_scenarios.py | 5 |

## 3. 必备知识 checklist

- [ ] 能画出会话模式和工单四态，解释为什么分开。
- [ ] 能解释 HTTP 401/403/404/409/422 的意义。
- [ ] 能复现两个客服拿同一版本修改，一次成功一次冲突。
- [ ] 能解释事务发件箱仍可能重复投递，以及消费如何去重。
- [ ] 能说清普通与紧急 SLA、等待客户时是否停表。
- [ ] 能自行添加一条场景样例并解释标签依据。

## 4. 推荐阅读

| 主题 | 通用技术点 | 建议阅读位置 | 预计时间 | 读完能回答什么 |
|---|---|---|---|---|
| 请求入口 | 身份、输入契约 | src/service_flow/api.py | 45 分钟 | 哪些参数不能信任客户端 |
| 多轮对话 | 有界窗口、角色消息 | src/service_flow/conversations.py | 60 分钟 | 哪些历史进入模型、哪些只持久化 |
| 工单 | 状态转换、SLA | src/service_flow/cases.py | 90 分钟 | 转交、重开与计时的关系 |
| 退款 | 事务、订单幂等 | src/service_flow/store.py | 60 分钟 | 两个确认令牌为什么不会重复退款 |
| 通知 | 原子写出、非原子投递 | src/service_flow/outbox.py | 90 分钟 | 发送后崩溃会发生什么 |
| 验证 | 合成集、边界与异常 | tests/test_enterprise.py；evaluation/datasets/support-v1.jsonl | 60 分钟 | 为什么通过率不能当线上准确率 |

## 5. 自学提醒

若某文件或原理看不懂，请继续追问 AI；本技能负责给学习路径与题目，不提供逐行讲解。学习时按“运行场景→预测数据库变化→读代码→改坏再跑测试”循环，先理解一条完整链路，再学名词。代码由 AI 协作完成，要把能独立修改与暂时只会使用的部分分开陈述。

## 6. 技术定位

后端与 AI 应用交叉：受控售后工作流承接知识问答、模型意图分类与人工处理。当前模型不自主规划工具调用；第二项目的长处是工程约束与业务闭环，而非复杂自主 Agent。

## 7. 核心原理

1. 追问缺少主语 → 保存主题与有界消息 → 会话服务补充检索语义；不是无限堆历史。
2. 人工接管后机器人误操作 → 会话模式与工单 FSM → 人工状态阻止自动退款与排障。
3. 两人看到相同页面 → 版本号条件更新 → 陈旧动作返回冲突；页面刷新后由人重新决定。
4. 提交业务后进程崩溃 → 同事务保存待投递事件 → 后台恢复投递，收件箱按事件 ID 去重。
5. 客服未及时回复 → 明确 SLA 截止时间 → 扫描和迟到回复检查记录违约事件。

## 8. 设计决策

| 备选 | 取舍 | 风险 | 验证 |
|---|---|---|---|
| LangGraph / 显式状态机 | 固定业务流程使用数据库状态机 | 分支增加后维护成本上升 | 状态转换测试；无图运行时中断 |
| Redis 队列 / SQLite Outbox | 本地部署减少依赖，原子保存事件 | 单库与共享锁限制吞吐 | 两连接领取测试，不等同分布式压测 |
| 无限历史 / 有界摘录 | 控制模型输入长度 | 较早信息可能被截断 | 窗口与预算测试 |
| 发送失败即丢弃 / 有限重试 | 短暂故障恢复，长期失败保留死信 | 无人处理的死信仍积压 | 回退、租约与主管重试测试 |

## 9. 量化与验证

已完成 55 项自动化测试和 60 条/72 轮合成回归；结果见 evaluation/scenario-results.json。建议自行增加未知表达留出集，报告分类型错误；压力、真实模型、多组织权限与支付沙箱待测。先运行 tests，再运行 scripts/evaluate_scenarios.py，最后按 scripts/browser-enterprise.cjs 复现待客户、转交、结案、重开。
