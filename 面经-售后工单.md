# 面经：售后工单

## 项目简介

面向消费电子售后，构建带有界对话上下文、人工接管和退款确认的工单系统；使用状态机、角色授权、SLA 与事务发件箱保证处理过程可追踪。代码为 AI 协作开发，下文是理解源码后的口播学习稿，不能直接当作个人已掌握的证明。

## 简历 bullet

- **流程编排：**面向排障反复追问与人工交接，设计持久化工单状态机（FSM），支持待接管、处理中、待客户补充、结案及主管重开；通过角色与负责人校验约束操作，人工阶段暂停自动动作。

- **上下文治理：**为避免多轮追问丢失主题，保存角色消息并按窗口与字符预算组装历史，用结构化快照交接订单、主题和近期交流；支持主题补全及排障失败转人工，原始记录尚未实现归档。

- **服务时限：**为区分普通问题和安全问题，建立技术、账务、安全队列与首次响应/解决 SLA，后台扫描超期并记录通知；转交不重置期限，等待客户继续计时，规则不等于线上可用性承诺。

- **请求可靠性：**为防止并发修改覆盖及业务成功后通知丢失，使用版本号条件更新和 Transactional Outbox，将工单状态、审计、事件同事务提交；投递支持租约、指数退避、死信重试及事件 ID 去重。

- **动作安全与验证：**为避免未确认退款和重复副作用，以 Pydantic 契约、限时确认与订单唯一账本限制写操作；通过 55 项测试与 60 条/72 轮合成场景回归，覆盖权限、SLA、事务回滚和退款去重，未接真实支付。

## 面试问题

### 业务建模与上下文

#### 1. 这个项目解决什么问题？

我把场景放在消费电子售后：用户先做知识排障，失败后需要人工继续，而不是每次重新描述问题。我的学习重点是把聊天、订单、工单和客服动作放进同一条链路。系统保留角色消息，转人工时保存订单、主题和最近交流，人工期间停止自动业务动作。客户可以补充信息，客服可以接管、转交、结案，主管可以重开。验证时我会从客户页发起问题，再到客服页完成处理并刷新页面检查记录。当前使用合成订单与本地退款，代码是借助人工智能协作完成的，我不会把它说成已经服务真实企业。

#### 2. 为什么把会话模式与工单状态分开？

我看到同一个聊天窗口既可能由机器人回答，也可能由人工处理，所以会话模式回答的是现在谁负责响应；工单状态回答的是业务进行到哪一步。比如等待客户补充时仍由人工负责，机器人不能自动退款；恢复自动服务后，原人工工单已经结束，聊天仍可以继续。因此系统分别保存这两类状态，并在事务中协调更新。我会用等待补充、客户回复、结案恢复三个场景检查组合是否合法。这样的拆分能避免用一个字段同时表达两种含义，但也带来同步约束，需要测试保证不会出现工单已结案却仍被旧客服修改的矛盾状态。

#### 3. 系统如何记住上一轮？

我把记忆理解为保存信息和挑选本轮需要的信息两件事。系统把原始角色消息存在数据库，每次构建上下文时只取最近八条，并限制单条和总字符数；更早的用户原话只保留有限摘录，另存当前主题。规则路径会用主题补全省略式追问，模型分类路径则收到这份有界历史。验证时我会先问蓝牙排障，再问具体怎么操作，检查知识引用是否保持相关。这里的压缩是窗口和截断，并不是语义摘要，也没有跨会话长期记忆；过长或远期信息可能丢失，所以我不能说它能完整记住所有历史。

#### 4. 为什么没有使用 LangGraph 中断？

我会先区分业务暂停和图执行中断。这里的业务流程比较固定，数据库保存自动服务、等待人工、人工处理和结案模式；后续请求根据状态决定能执行哪些动作，进程重启后仍能读取这些记录。这已经能实现人工接管期间暂停自动操作，但没有调用图框架的中断与恢复接口。我验证的是状态持久化、权限和接管后的动作拦截。未来如果加入动态多步规划，可以评估图框架及持久化检查点；不过即便换框架，退款授权、数据库事务和幂等仍必须保留，框架不能自动保证业务安全。

#### 5. 模型在其中有多少自主权？

我会把当前系统描述为受控工作流中的可选模型分类，而不是完全自主的工具智能体。显式安全、退款和人工诉求先经过规则，其他输入可以交给兼容接口的模型判断意图，异常时回退规则。具体工具的参数与执行顺序由业务代码约束，退款金额来自订单，提交前仍需要用户确认。验证时我会检查模型收到历史背景，也会构造网络失败和危险输入，确认不能绕过本地限制。当前没有用真实外部模型验证线上效果，知识回答仍主要引用手册内容，这个边界比把普通路由夸成自主规划更容易解释清楚。

**追问 1：摘要截断会丢失关键条件怎么办？**

我承认固定窗口不能保证保留全部关键条件，所以会先区分可从订单重新读取的事实、会话中的主题和用户自由表达。金额与订单归属不依赖模型记忆，而是执行时从可信数据重新核验；早期用户描述被截断时，不应该据此自动扩大业务权限。我的验证方式是构造超长历史，检查预算是否受控，再确认退款仍必须过确认门。后续可以增加结构化事实提取及冲突处理，但必须保留来源和更新时间，不能只换一个记忆框架就声称问题解决。当前实现的价值是输入可控和动作边界明确，长期语义记忆仍是扩展方向。

**追问 2：人工接管时客户再次要求退款怎么办？**

我会把这条消息当作人工阶段的补充材料保存，并提示已经转交客服，而不会重新进入自动退款路径。原因是工单处理时可能正在核查故障和政策，机器人与人工同时修改订单会产生竞争。系统还会检查同一订单在其他会话中是否处于人工处理，避免客户另开页面绕过接管限制。验证时我会先取得退款提案，再让另一会话转人工，随后提交旧提案，预期被阻止。这个结果说明授权要在真正执行时重新检查，而不是只在生成提案时检查一次；接管状态本身也是执行前置条件。

### 一致性、权限与动作安全

#### 6. 两个客服同时修改会怎样？

我会先说明页面上看到的工单只是某个时刻的快照。两个客服或两个标签页读到同一个版本后，如果直接覆盖数据库，后提交的人可能抹掉前一个人的结果。系统要求修改请求携带版本，在数据库写事务中比对当前版本，通过后更新并增加版本；陈旧请求返回冲突，让页面刷新后重新决策。我还要检查当前负责人，因为版本一致并不等于有权限。测试分别覆盖旧版本拒绝和转交后旧负责人拒绝，确保没有额外消息或状态写入。当前共享锁与数据库事务适合单节点，不能把这个局部保证扩张成已经具备大规模分布式并发能力。

#### 7. 身份验证和权限判断有什么区别？

我用登录凭据回答请求者是谁，用角色、负责人和主管权限回答他能做什么。客户只能查看自己的订单与会话，客服才能进入工单后台；一般客服操作当前负责的工单，主管拥有重开和死信重试能力。不能信任客户端随意提交的客户编号，可信身份应从服务端凭据映射读取。验证时我会用另一个客户查会话，用普通客服重开工单，再用主管执行相同动作，比较拒绝与成功结果。当前开发登录只用于合成身份演示，正式接入仍需要身份网关、令牌管理和组织隔离，已有角色校验并不等于完整企业登录系统。

#### 8. 退款如何防止重复？

我把用户确认和副作用去重分开。首先用限时随机令牌绑定客户、订单和会话，确认时重新检查金额、状态和人工接管情况；其次以订单作为本地账本唯一键，在写事务中完成检查、插入与订单更新。这样同一个令牌重试或者两个不同令牌同时确认，都不能给同一订单写出两条退款记录。测试使用独立数据库连接并发执行，而不是仅在单个函数里重复调用。当前副作用只是本地模拟账本，如果接真实支付，需要传支付方幂等键、查询交易结果并做对账，不能认为本地事务会把外部网络请求一起回滚。

#### 9. 为什么结案要分类和说明？

我把结案看成业务结果，而不是把一个状态从开改成关。客服需要说明做过什么，并选择问题解决、维修处理、政策说明或恢复自动服务等分类，这样用户和后续接手人能理解工单结束的原因。系统在接口层要求分类，数据库事务保存状态、审计与事件，主管重开时保留原有处理历史。验证时我会提交缺少分类的请求，预期拒绝，再正常结案并查看审计时间线。当前分类只是本地业务标签，不代表已经创建真实维修单；以后对接维修或物流时，应把实际外部单号和最终结果纳入同一条可核验的记录链路。

#### 10. SLA 如何设计？

我把首次响应时间和问题解决时间分开，因为客服点击接管并不等于真正回复客户。普通工单设置三十分钟首次响应、八小时解决，安全工单使用更短时限；首次人工回复停止响应计时，转交不重置时间，等待客户仍继续计算解决时限。后台定期扫描超期，迟到回复也当场检查，避免扫描间隔遗漏。测试使用可控时钟推进时间，验证超期事件只生成一次，主管重开后重新计时。这些数字是项目中的业务配置，不是已经对外履约的数据；工作日历、节假日和暂停计时政策还没有实现，接企业前必须与业务方明确。

**追问 1：只在前端禁用按钮够吗？**

我不会把前端按钮当成权限边界，因为用户可以直接构造请求或使用旧页面。页面禁用只能帮助操作员理解当前状态，真正的检查必须在服务端，每次操作都核验角色、负责人、状态和版本。比如转交后旧客服页面即使没刷新，请求也必须被拒绝；普通客服即使伪造主管按钮，也不能重开工单。我的验证会同时覆盖接口返回和浏览器按钮变化，前者验证安全性，后者验证可理解性。现在客户端不直接指定可信客户身份，服务端从凭据中取身份，但开发演示登录本身仍不适合开放到公网，需要单独替换。

**追问 2：等待客户为什么不停 SLA？**

我会先说明这是一条明确的项目规则，而不是所有企业都必须如此。当前用连续自然时间计算解决期限，等待客户补充也继续计时，目的是保持实现简单、让超期行为容易核验。如果业务要求暂停，就需要记录每一段暂停区间和恢复时间，处理重复暂停、重启恢复与转交，并重新定义统计口径。我会用假时钟测试等待期间的超期标记，再测试客户补充后恢复处理而不重置截止时间。当前实现没有偷偷声称支持工作日历；面试时我会把已有规则、选择原因和未来修改所需的数据结构清楚区分。

### 可靠性与验证

#### 11. 为什么需要事务发件箱？

我考虑的故障是工单已经改成功，但进程在发送通知前崩溃。如果只在提交后调用通知接口，恢复时可能不知道还有消息没发。系统把工单状态、审计和待发送事件放进同一数据库事务，提交后后台再领取事件投递；这样业务成功就有持久化待办，失败则一起回滚。测试会在写事件时主动抛出异常，确认状态没有半更新。当前通知发送到本地收件箱，未接外部邮件或消息服务；设计解决的是业务提交与通知意图之间的一致性，而不是保证网络世界的每一步都只执行一次。

#### 12. 为什么有 Outbox 还需要幂等消费？

我会举消费成功但确认丢失的情况：收件箱已经收到事件，worker 却在标记发送成功前崩溃，租约到期后另一个 worker 会再次领取并投递。如果消费者没有去重，就会显示两条相同通知。当前收件箱用事件编号作为唯一键，重复插入不产生新记录，所以允许至少一次投递，同时保持本地消费效果一次。测试会先成功写收件箱再抛出异常，随后推进时间重试，检查最终只有一条记录。这个保证依赖消费者的幂等设计；如果换成邮件服务或支付系统，需要相应的幂等契约，不能直接复制这个结论。

#### 13. 租约和重试解决什么？

我把租约看成有限时间的领取资格。worker 领取事件时增加尝试次数并保存租约令牌，正常完成后带令牌确认；如果卡住或崩溃，租约到期允许其他 worker 接手，旧 worker 迟来的确认不能覆盖新任务状态。短暂失败按指数退避重试，达到次数上限后进入死信，由主管检查后重新排队。测试覆盖过期领取、旧确认无效、两个连接不能同时拿到同一事件和失败后重试成功。当前参数针对本地实验，真实接入要结合服务超时、处理耗时及告警策略调整，不能无限重试掩盖永久业务错误。

#### 14. 测试集如何构建、通过率说明什么？

我按业务风险组织六十条合成会话，包含七十二轮输入，给每轮标注预期路由、响应状态以及需要时的知识来源，并检查未确认退款没有账本副作用。每条在独立临时数据库执行真实会话逻辑，避免前一条用例退款后污染下一条。当前全部通过说明已知场景回归稳定，但开发时看过标签，所以它不是独立泛化评测。我还用单元和接口测试覆盖版本冲突、事务失败、租约恢复与权限，再用真实浏览器跑工单闭环。接真实模型后应冻结配置并补人工审核留出集，分风险报告错误，不能只宣传一个总准确率。

#### 15. 离企业生产还差什么？

我会先肯定当前可核验的部分：持久化工单、角色授权、版本冲突、服务时限、事务发件箱和回归测试都已经实现。但真实企业还需要统一登录、多组织隔离、真实订单与支付、监控告警、数据保留政策和容量验证。当前共享锁会串行化会话，模型请求也可能占用处理链路，因此不能声称高并发；开发身份和合成订单也不是企业业务。我的下一步会先接一条真实沙箱业务链路和可靠身份边界，再用负载数据决定是否换数据库、拆投递服务或引入队列，而不是为了名词先堆中间件。

**追问 1：能否把数据库换成 PostgreSQL 就说高并发？**

我不会这样判断，因为吞吐受锁粒度、同步模型请求、连接池、外部服务速度和幂等策略共同影响。当前共享锁串行化会话，即使底层数据库更强，应用仍可能逐个等待模型调用。我的改进顺序会是测量请求耗时，分离数据库短事务和外部等待，建立会话级并发控制，再测试不同用户与同一订单的竞争。数据库迁移还要重新验证事务隔离与唯一约束，不能只改连接字符串。现在没有压力测试数据，所以面试中我只说明已验证的局部并发保证，并把多节点容量列为待测，而不是给出没有依据的吞吐指标。

**追问 2：如何证明你理解而不只是运行 AI 代码？**

我会把答案落实为可以当场操作的验证：先画出一条工单从自动排障到主管重开的状态变化，再预测每次动作会更新哪些记录；接着故意提交旧版本，解释为什么收到冲突，然后制造通知发送后确认失败，检查重试与去重。对于不理解的代码，我会先阅读测试和关键事务，再独立修改一个小规则并新增案例，而不是背诵类名。这个项目确实借助人工智能开发，当前文档是学习口播稿，不证明我已经掌握全部模块。真正能体现理解的是解释取舍、定位失败并独立验证修改，不能冒充实际企业项目经历。

## 源码证据索引

| 主题 | 关键路径与内部符号 | 对应正文位置 |
|---|---|---|
| 会话与上下文 | src/service_flow/conversations.py / ConversationService | 1—5 |
| 规则与模型 | src/service_flow/routing.py | 5 |
| 工单与 SLA | src/service_flow/cases.py / CaseService.action、scan_sla | 6、9、10 |
| 角色授权 | src/service_flow/api.py / principal、case_action | 7 |
| 退款账本 | src/service_flow/store.py / execute_refund | 8 |
| 事务事件与投递 | src/service_flow/outbox.py / publish、OutboxDispatcher | 11—13 |
| 测试与场景集 | tests/test_enterprise.py；scripts/evaluate_scenarios.py；evaluation/datasets/support-v1.jsonl | 14—15 |

## 交接事实

给简历改写：目标为 AI 应用开发实习；项目代码和验证由 AI 协作完成，个人独立掌握程度需以复现与讲解为准。可写状态机、版本冲突、SLA、Outbox、55 项测试及 60 条/72 轮合成场景；不可写企业已上线、真实支付或生产吞吐。

给模拟面试：重点追问个人具体修改边界、测试集是否留出、CAS 与互斥锁区别、事务发件箱重复投递、SLA 停表政策，以及缺失的真实业务与容量验证。所有“主导”“线上提升”“准确率”均需额外证据。
